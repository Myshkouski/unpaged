{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^8.1.9"
  },
  "scripts": {
    "build": "lerna run build",
    "publish:from-git": "lerna publish from-git --yes",
    "version:release": "lerna version --conventional-commits --conventional-graduate --yes",
    "version:beta": "lerna version --conventional-commits --conventional-prerelease --preid beta --yes"
  }
}
